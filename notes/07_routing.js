// whenever we are using react-router dom always make sure your entire application should be inside browser router

// <BrowserRouter>, <Routes> , <Route>

// route have two thing , one is path , and one is elment

// navigate()

// for simple redirect -> use link
// if we want to do some  task before re-directing. and then want to re-direct. then use navigate()

// Lazyloading...
//while doing lazyloading 
// import statement, suspense, fallback(alternative until original is not displayed.)
